"use client";
import Link from "next/link";
import { Menu, Moon, Sun, X } from "lucide-react";
import { useEffect, useState } from "react";
const links = [["À propos", "ABOUT"], ["Services", "service"], ["Expérience", "EXPERIENCES"], ["Projets", "PROJECT"], ["Contact", "CONTACT"]];
export default function Nav() { const [open, setOpen] = useState(false); const [dark, setDark] = useState(false); useEffect(() => { const saved = window.localStorage.getItem("portfolio-theme"); const shouldBeDark = saved ? saved === "dark" : window.matchMedia("(prefers-color-scheme: dark)").matches; document.documentElement.classList.toggle("dark", shouldBeDark); setDark(shouldBeDark); }, []); const toggleTheme = () => { const next = !dark; document.documentElement.classList.toggle("dark", next); window.localStorage.setItem("portfolio-theme", next ? "dark" : "light"); setDark(next); }; return <header className="fixed inset-x-0 top-0 z-50 border-b border-white/10 bg-[#0a1513]/85 text-white backdrop-blur-xl"><div className="mx-auto flex h-[74px] max-w-7xl items-center justify-between px-5 sm:px-8 lg:px-12"><Link href="#home" className="group flex items-center gap-3" onClick={() => setOpen(false)}><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#c5f54a] text-sm font-black text-[#132000] transition group-hover:rotate-3">AE</span><span className="hidden text-sm font-bold tracking-tight sm:block">Abdelmounaim<span className="text-[#c5f54a]">.</span></span></Link><nav className="hidden items-center gap-7 md:flex">{links.map(([label, href]) => <Link key={href} href={"#" + href} className="text-sm font-semibold text-white/70 transition hover:text-[#c5f54a]">{label}</Link>)}</nav><div className="flex items-center gap-2"><a href="https://github.com/Abdelmounaim08" target="_blank" rel="noreferrer" className="hidden rounded-full border border-white/15 px-4 py-2 text-xs font-bold text-white/80 transition hover:border-[#c5f54a]/60 hover:text-[#c5f54a] sm:block">GitHub ↗</a><button type="button" onClick={toggleTheme} aria-label={dark ? "Activer le thème clair" : "Activer le thème sombre"} className="grid h-10 w-10 place-items-center rounded-full border border-white/15 text-white/80 transition hover:border-[#c5f54a]/60 hover:text-[#c5f54a]">{dark ? <Sun className="h-4 w-4" /> : <Moon className="h-4 w-4" />}</button><button type="button" onClick={() => setOpen(!open)} aria-label={open ? "Fermer le menu" : "Ouvrir le menu"} className="grid h-10 w-10 place-items-center rounded-full border border-white/15 text-white md:hidden">{open ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />}</button></div></div>{open && <div className="border-t border-white/10 bg-[#0a1513] px-5 py-5 md:hidden"><nav className="flex flex-col gap-1">{links.map(([label, href]) => <Link key={href} href={"#" + href} onClick={() => setOpen(false)} className="rounded-xl px-3 py-3 text-sm font-semibold text-white/80 hover:bg-white/5 hover:text-[#c5f54a]">{label}</Link>)}</nav></div>}</header>; }
